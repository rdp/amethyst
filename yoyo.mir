interface MyInterface do
  def run(a:int):int; end
end

def go(block:MyInterface)
  block.run(3)
end

# how does it parse this? apparently the block yields no return value....hmm...

go { 4 }
