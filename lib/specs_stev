# same dir means same package ja?
# require 'go2.mir'

# specs :P

#RubyInt.new(1) - RubyInt.new(2)
#RubyInt.new(1) + RubyInt.new(2)

RubyInt.new(1).times do |n| 
  3
  nil
end

RubyInt.new(1).times do |n| 
  RubyInt.new(1)
end

puts 'specs done'